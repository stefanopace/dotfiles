PS1='\[\e[01m\]\
`if [ $? != 0 ]; then
	echo \[\e[31m\];
else
	echo \[\e[32m\];
fi`\
`if [ $(whoami) == "stefano" ]; then
	echo "|>";
elif [ $(whoami) == "root" ]; then
	echo "+>"
else
	echo "?>";
fi`\
\[\e[00m\]'
