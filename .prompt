withColorForStatusCode () {
	return ''
}
PS1='`if [ $? != 0 ]; then
	echo -n \[\e[31m\];
else
	echo -n \[\e[32m\];
fi
if [ $(whoami) == "stefano" ]; then
	echo -n "|>";
elif [ $(whoami) == "root" ]; then
	echo -n "+>";
else
	echo -n "?>";
fi`\[\e[00m\]'
