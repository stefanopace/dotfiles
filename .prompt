# vim syntax=sh
PS1='\[\e[` [ $? == 0 ] && echo '32' || echo '31' `m\]`
printf '%.s-' $(seq 1 $(tput cols))
`\n\[\e[00m\]`
d | sed -E "s|([^/]+)|\[\e[94m\]\1\[\e[00m\]|g"
case $(whoami) in
    "stefano") echo ">"  ;;
       "root") echo "#>" ;;
            *) echo "??>";;
esac `'
