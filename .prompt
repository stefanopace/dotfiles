# vim syntax=sh
PS1='`
PATHS[1]="/home/stefano"
ICONS[1]="~"
PATHS[2]="~/dotfiles"
ICONS[2]="•"
PATHS[3]="~/projects"
ICONS[3]="p"
PATHS[4]="p/devbox"
ICONS[4]="❐"
PATHS[5]="❐/nap/htdocs/dev"
ICONS[5]="ℬ"
PATHS[6]="~/downloads"
ICONS[6]="↓"
PATHS[7]="~/Scrivania/work-in-progress"
ICONS[7]="w"
PATHS[8]="~/Scrivania/utilities/deploy"
ICONS[8]="↑"

pwd |
while read pwd; do
  str=$pwd
  for i in $(seq 1 ${#PATHS[@]}); do
    str=$(echo $str | sed "s|${PATHS[$i]}|${ICONS[$i]}|");
  done;
  echo $str;
done | sed -E "s|/(.)[^/]*|\1|g"`\[\e[`[ $? == 0 ] && echo '32'\
                                                   || echo '31'`m\]`case $(whoami) in
                                                                        "stefano") echo ">"  ;;
                                                                           "root") echo "#>" ;;
                                                                                *) echo "??" ;;
                                                                    esac`\[\e[00m\]'
